{
  "name": "ops-workflow-mcp",
  "version": "1.0.0",
  "description": "MCP server for the ops-workflow plugin — bridges Claude Desktop/Cowork to Claude Code's ops capabilities",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:check": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.77"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@types/node": "^22.19.15",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3",
    "zod": "^4.3.6"
  }
}
